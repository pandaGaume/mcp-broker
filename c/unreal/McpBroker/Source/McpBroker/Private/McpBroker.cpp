// Module entry point. Nothing to do at load: the link is created by the
// subsystem when an application calls Connect.

#include "Modules/ModuleManager.h"

IMPLEMENT_MODULE(FDefaultModuleImpl, McpBroker)
