{
    "name": "@cyanmycelium/mcp-broker-workspace",
    "version": "0.0.0",
    "private": true,
    "description": "npm workspace root for the CyanMycelium MCP broker and its client-side tunnel transports.",
    "license": "Apache-2.0",
    "author": "Guillaume Pelletier (CyanMycelium)",
    "type": "module",
    "engines": {
        "node": ">=20.11.0"
    },
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "//build": "Dependency order, not folder order: `npm run --workspaces` walks packages alphabetically, and the broker compiles against the provider's published dist. Add new packages here, downstream last.",
        "build": "npm run build --workspace @cyanmycelium/mcp-broker-provider && npm run build --workspace @cyanmycelium/mcp-broker",
        "clean": "npm run clean --workspaces --if-present",
        "compile": "npm run compile --workspaces --if-present",
        "//typecheck": "Covers what `compile` cannot: tsconfig.build.json excludes tests, and vitest strips types without checking them, so test files are otherwise never verified.",
        "typecheck": "npm run typecheck --workspaces --if-present",
        "test": "npm run test --workspaces --if-present",
        "lint": "npm run lint --workspaces --if-present",
        "lint:fix": "npm run lint:fix --workspaces --if-present",
        "format": "npm run format --workspaces --if-present",
        "format:fix": "npm run format:fix --workspaces --if-present"
    }
}
